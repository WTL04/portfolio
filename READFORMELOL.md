For version control:
- ensure you are on `main` branch
- commit and push to `main` branch 

For github pages depolyment:
- run `npm run build` to ensure there are no errors
- run `npm run depoly` for vite to deploy automatically to `gh-pages` branch
